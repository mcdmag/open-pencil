# `batch` tool is registered in MCP server: run `bun test tests/engine/mcp-server.test.ts` — the "batch tool is listed" test passes

DoD: batch registered.

```mermaid
flowchart LR
    A[batch in listTools] --> B[✓]
```
