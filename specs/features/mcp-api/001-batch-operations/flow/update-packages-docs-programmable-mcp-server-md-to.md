# Update `packages/docs/programmable/mcp-server.md` to update `create_shape` docs with inline style params

create_shape docs updated.

```mermaid
flowchart LR
    A[mcp-server.md] --> B[create_shape with inline styles]
```
